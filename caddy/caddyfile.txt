:8080 {
    # سرو کردن فرانت‌اند
    root * ./frontend/dist
    file_server

    # برای SPA مثل React یا Vue
    try_files {path} /index.html

    # پروکسی کردن درخواست‌های API
    handle_path /api/* {
        reverse_proxy localhost:3001
    }
}
